name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install packages
      run: npm install
    - name: Check Lint and Tests
      run: |
        npm run autofix
        npm run test

