name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/typescript-action@v1
    - name: Run ESLint
      run: npm run autofix
    - name: Run Test
      run: npm run test

