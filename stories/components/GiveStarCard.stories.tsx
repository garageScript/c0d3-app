import React from 'react'
import GiveStarCard from '../../components/GiveStarCard'

export default {
  component: GiveStarCard,
  title: 'Components/GiveStarCard'
}

export const Primary: React.FC = () => <GiveStarCard show={true} />
